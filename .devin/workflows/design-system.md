---
description: Design System — генерация и управление дизайн-системой с токенами
---

Activate the Design System skill for creating and managing design systems with tokens.

1. Read the skill file at `~/.windsurf/skills/design-system/SKILL.md` to load design system instructions.
2. If needed, read reference files from `~/.windsurf/skills/design-system/references/` for:
   - `token-architecture.md` — token hierarchy and structure
   - `primitive-tokens.md` — raw design tokens
   - `semantic-tokens.md` — semantic token mapping
   - `component-tokens.md` — component-level tokens
   - `component-specs.md` — component specifications
   - `states-and-variants.md` — states and variant patterns
   - `tailwind-integration.md` — Tailwind CSS integration
3. Generate a complete design system with proper token architecture, component specs, and Tailwind integration.

The user's request after the slash command is the task to execute:
$ARGUMENTS
