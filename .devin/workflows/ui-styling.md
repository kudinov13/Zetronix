---
description: UI Styling — Tailwind/shadcn стилизация компонентов с дизайн-токенами
---

Activate the UI Styling skill for component styling with Tailwind CSS and shadcn/ui.

1. Read the skill file at `~/.windsurf/skills/ui-styling/SKILL.md` to load styling instructions.
2. If needed, read reference files from `~/.windsurf/skills/ui-styling/references/` for:
   - `tailwind-customization.md` — Tailwind config and theme
   - `tailwind-responsive.md` — responsive design patterns
   - `tailwind-utilities.md` — utility class reference
   - `shadcn-components.md` — shadcn/ui component library
   - `shadcn-theming.md` — shadcn theme customization
   - `shadcn-accessibility.md` — accessibility guidelines
   - `canvas-design-system.md` — design system tokens
3. Apply the guidelines to style components with proper design tokens, responsive behavior, and accessibility.

The user's request after the slash command is the task to execute:
$ARGUMENTS
